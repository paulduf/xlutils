set working-directory := '..'

# Run all tests
all *args:
    uv run pytest tests/ {{args}}

# Run tests with verbose output
verbose:
    uv run pytest tests/ -v

# Run a specific test file
file name:
    uv run pytest tests/{{name}} -v
